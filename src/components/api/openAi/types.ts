export interface MovieItem {
  uuid: string;
  title: string;
  description: string;
  description_long: string;
  is_active: boolean;
  image_url: string;
  image_file: string;
  created_at: string;
}
